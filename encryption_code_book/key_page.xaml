<Page
    x:Class="encryption_code_book.key_page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Background="Blue">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{x:Bind view.prompt,Mode=OneWay}" VerticalAlignment="Bottom"/>
            <TextBox Grid.Row="1" Text="{x:Bind view.key,Mode=TwoWay}" VerticalAlignment="Top"/>
            <AppBarButton Grid.Row="1" Icon="Forward" HorizontalAlignment="Right" Click="confirm"/>
        </Grid>
    </Grid>
</Page>
