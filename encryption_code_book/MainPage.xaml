<Page
    x:Class="encryption_code_book.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:encryption_code_book"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
         
        <Grid>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Content="展开" HorizontalAlignment="Left" VerticalAlignment="Top" Click="button_click" />
                <SplitView Grid.Row="1" x:Name="split_view" DisplayMode="CompactOverlay">
                    <SplitView.Pane>
                        <StackPanel Orientation="Vertical">
                            <Button Content="文  私密文本"></Button>
                            <Button Content="密  密码本"></Button>
                        </StackPanel>
                    </SplitView.Pane>
                </SplitView>
            </Grid>
        </Grid>
    </Grid>
</Page>
